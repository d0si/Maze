# TODO:
Get value by ref -> change it -> verify in original element => for each type
Equals function could be useful for verifying if two elements are the same => Will be a problem if one needs to check if two elements are also same instance
