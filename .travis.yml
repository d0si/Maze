language: cpp
os: linux
compiler: gcc

script:
    - mkdir -p build
    - cd build
    - cmake .. -DCMAKE_BUILD_TYPE=Debug -DMAZE_BUILD_TESTS=ON -DMAZE_CODE_COVERAGE=ON
    - make -j4
    - make MazeTest_coverage
